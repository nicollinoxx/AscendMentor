<%= turbo_stream.replace :next_page_container, partial: "identity/profiles/profile", locals: { chat: @chat }, collection: @page.records, as: :user %>

<% unless @page.last? %>
    <%= turbo_stream.append :profiles, partial: "identity/profiles/next_page_container", locals: { page: @page.next_param } %>
<% end %>
